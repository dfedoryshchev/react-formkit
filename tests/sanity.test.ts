import { describe, it, expect } from 'vitest'

// Smoke test confirming the test runner is wired up. Real suites follow.
describe('test setup', () => {
    it('runs', () => {
        expect(true).toBe(true)
    })
})
