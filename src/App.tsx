import React from 'react'

const App = () => {
    return (
        <div style={{ maxWidth: 500, margin: '40px auto', padding: '0 20px' }}>
            <h1>react-formkit</h1>
            <p>Run the dev playground for a demo:</p>
            <code>npx vite dev/</code>
        </div>
    )
}

export default App
